# Production env vars (Cloudflare Workers Builds). Set these under the Worker's
# Settings > Build > Variables and secrets, as BUILD-time variables (not 'vars').

# Render backend URL. The trailing slash is required: request paths are
# joined as `${API_URL}${endpoint}` and endpoints carry no leading slash.
VITE_API_URL=https://your-service.onrender.com/
