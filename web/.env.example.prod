# Production env vars (Cloudflare Pages). Set these under
# Settings > Environment variables > Production.
#
# Vite inlines these at build time, not runtime. Changing this value
# requires a rebuild/redeploy to take effect.

# Your Railway backend URL. No trailing slash.
VITE_API_URL=https://your-service.up.railway.app
